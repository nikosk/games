{
  "name": "koko-crocodile-story",
  "version": "1.0.0",
  "private": true,
  "description": "Η Κόκο και το χαμένο καπέλο — διαδραστική ιστορία για tablet",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "check": "node --check game.js && node --test"
  }
}
