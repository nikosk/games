import Phaser from 'phaser';import {KitchenScene} from './scenes/KitchenScene';
const game=new Phaser.Game({type:Phaser.AUTO,parent:'game',width:innerWidth,height:innerHeight,backgroundColor:'#b8cf9f',scene:[KitchenScene],scale:{mode:Phaser.Scale.RESIZE,autoCenter:Phaser.Scale.NO_CENTER},render:{antialias:true,pixelArt:false,roundPixels:true},input:{activePointers:2}});game.canvas.tabIndex=0;game.canvas.setAttribute('role','application');game.canvas.setAttribute('aria-label',"Little Chef recipe builder: make Tilly's tomato-cheese toast");addEventListener('beforeunload',()=>game.destroy(true),{once:true});
